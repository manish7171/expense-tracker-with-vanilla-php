<!DOCTYPE html>
<html>

<head lang="en">
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <title>Untitled</title>
  <meta name="description" content="This is an example of a meta description.">
</head>

<body>
  <h1>Home Page</h1>
  <hr />
  <div>
    {% for i in invoices %}
        Invoice ID: {{(i['id'])}} <br />
        Invoice Amount: {{ (i['amount'])}} <br />
    {% else %}
     <div>No record found</div>
    {% endfor %}
  </div>

</body>

</html>
